// main7.js
// ЛР7: загрузили блюда -> отрисовали -> остальной код работает как раньше

(async function () {
  try {
    const dishes = await window.loadDishes();
    window.renderMenu(dishes);

    // если у тебя фильтры требуют инициализацию — вызывай тут
    // initFilters(dishes);

    // если корзина/клики цепляются через делегирование — она уже работает
    // если корзина ищет карточки при старте — убедись, что инициализация идёт ПОСЛЕ renderMenu

  } catch (e) {
    console.error(e);
    alert("Не удалось загрузить меню. Проверь API / интернет / консоль.");
  }
})();
